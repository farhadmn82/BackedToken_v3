import { ethers } from "hardhat";

async function main() {
  // Placeholder deploy script
  console.log("Deploy script placeholder");
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
